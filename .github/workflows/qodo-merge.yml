name: qodo-merge
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  qodo-merge:
    if: ${{ secrets.QODO_API_KEY != "" }}
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read
    steps:
      - name: Qodo Merge Review
        # Pinned to v1 stable branch. For stricter pinning, replace with a commit SHA.
        uses: qodo-ai/qodo-merge@v1
        env:
          QODO_API_KEY: ${{ secrets.QODO_API_KEY }}
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
